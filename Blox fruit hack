
-- =============================================
-- BLOX FRUIT BANANA MENU V2 - ULTIMATE MOBILE
-- SUPPORT: Delta X, VNG, Arceus X, Hydrogen, Vega X
-- =============================================

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Camera = workspace.CurrentCamera

-- ===== SETTINGS =====
local Settings = {
    FlySpeed = 60,
    WalkSpeed = 90,
    JumpPower = 90,
    FarmRadius = 400,
    AttackDelay = 0.2,
    SeaBeastHeight = 30,
    V4Delay = 3,
    ChestDelay = 1.5,
    BountyDelay = 0.3,
    FruitDelay = 2,
    LeviSearchInterval = 5,
    AutoHideDelay = 10,
    FloatingButtonSize = 50
}

-- ===== STATE =====
local State = {
    ESP = false,
    Fly = false,
    Speed = false,
    InfiniteJump = false,
    NoClip = false,
    AutoFarm = false,
    AutoV4 = false,
    FindLevi = false,
    AutoSeaBeast = false,
    AutoRaid = false,
    AutoChest = false,
    AutoBounty = false,
    AutoFruit = false,
    AutoDungeon = false,
    AutoAwaken = false,
    AutoQuest = false,
    AutoCollect = false,
    AutoHatch = false,
    AutoFishing = false,
    AutoBoss = false,
    AutoElite = false,
    AutoParty = false,
    AutoTrade = false,
    AutoStock = false,
    AutoMastery = false,
    AutoBeli = false,
    AutoFragments = false,
    AutoLevel = false,
    AutoStats = false,
    AutoBuff = false,
    AutoHeal = false,
    AutoRejoin = false,
    AntiStun = false,
    AntiBan = false,
    AntiAFK = false,
    AntiLag = false,
    WaterWalk = false,
    NoFall = false,
    KillAura = false,
    Aimbot = false,
    MenuVisible = true,
    FloatingButton = true
}

local ESPObjects = {}
local FlyBV = nil
local FlyBG = nil
local Threads = {}
local Keybinds = {}
local MenuVisible = true
local FloatingButton = nil
local Dragging = false
local DragStart = nil
local DragOffset = nil

-- ===== NOTIFICATION =====
local function Notify(text, dur)
    dur = dur or 3
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "🍌 BANANA MENU",
            Text = tostring(text),
            Duration = dur
        })
    end)
end

-- ===== TELEPORT =====
local function Teleport(pos)
    if Character and Character:FindFirstChild("HumanoidRootPart") then
        Character.HumanoidRootPart.CFrame = CFrame.new(pos)
        return true
    end
    return false
end

-- ===== FIRE CLICK =====
local function FireClick(obj)
    if obj and obj:IsA("ClickDetector") then
        fireclickdetector(obj)
        return true
    end
    return false
end

-- ===== GET NEAREST NPC =====
local function GetNearestNPC(range, ignorePlayers, onlyBoss)
    local closest, dist = nil, range
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Model") and v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") then
            if ignorePlayers and v.Name == Player.Name then continue end
            if onlyBoss and not string.find(string.lower(v.Name), "boss") and not string.find(string.lower(v.Name), "dragon") and not string.find(string.lower(v.Name), "phoenix") then continue end
            if v.Humanoid.Health > 0 then
                local mag = (v.HumanoidRootPart.Position - Character.HumanoidRootPart.Position).Magnitude
                if mag < dist then
                    closest, dist = v, mag
                end
            end
        end
    end
    return closest, dist
end

-- ===== GET NEAREST PLAYER =====
local function GetNearestPlayer(range)
    local closest, dist = nil, range
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= Player and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            local mag = (v.Character.HumanoidRootPart.Position - Character.HumanoidRootPart.Position).Magnitude
            if mag < dist and v.Character.Humanoid.Health > 0 then
                closest, dist = v.Character, mag
            end
        end
    end
    return closest, dist
end

-- ===== EQUIP BEST TOOL =====
local function EquipBestTool(type)
    local best = nil
    local tools = {}
    
    for _, tool in pairs(Player.Backpack:GetChildren()) do
        if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
            table.insert(tools, tool)
        end
    end
    
    for _, tool in pairs(Character:GetChildren()) do
        if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
            table.insert(tools, tool)
        end
    end
    
    local patterns = {
        sword = {"sword", "blade", "katana", "saber", "cutlass", "triple", "dark", "pole", "trident", "yoru", "shisui"},
        fruit = {"fruit", "gomu", "mera", "pika", "sun", "dragon", "phoenix", "dark", "rumble", "venom", "dough", "soul", "leopard", "buddha"},
        gun = {"gun", "pistol", "rifle", "cannon", "bazooka", "shotgun", "sniper"},
        melee = {"superhuman", "death", "god", "electric", "shark", "gorilla"}
    }
    
    for _, tool in pairs(tools) do
        local name = string.lower(tool.Name)
        for _, pattern in pairs(patterns[type] or {}) do
            if string.find(name, pattern) then
                if not best or tool.Name > best.Name then
                    best = tool
                end
                break
            end
        end
    end
    
    if best then
        Humanoid:EquipTool(best)
        return best
    end
    return nil
end

-- ===== ATTACK =====
local function AttackTarget(target)
    if not target or not target:FindFirstChild("HumanoidRootPart") or not target:FindFirstChild("Humanoid") or target.Humanoid.Health <= 0 then
        return
    end
    
    Teleport(target.HumanoidRootPart.Position + Vector3.new(0, 4, 0))
    wait(0.05)
    
    local tool = EquipBestTool("sword") or EquipBestTool("fruit") or EquipBestTool("gun") or EquipBestTool("melee")
    if tool then
        for i = 1, 3 do
            tool:Activate()
            wait(Settings.AttackDelay)
        end
    end
end

-- ===== ESP =====
local function CreateESP(part, color, text)
    if not part or not part:IsA("BasePart") then return end
    
    local bill = Instance.new("BillboardGui")
    bill.Name = "ESP_Mobile"
    bill.Size = UDim2.new(0, 200, 0, 40)
    bill.AlwaysOnTop = true
    bill.Adornee = part
    
    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.new(0, 0, 0)
    bg.BackgroundTransparency = 0.4
    bg.Parent = bill
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = color or Color3.new(1, 0, 0)
    label.TextStrokeTransparency = 0.3
    label.TextScaled = true
    label.Font = Enum.Font.GothamBold
    label.Text = text or "Unknown"
    label.Parent = bill
    
    bill.Parent = part
    table.insert(ESPObjects, bill)
    return bill
end

local function ClearESP()
    for _, obj in pairs(ESPObjects) do
        pcall(function() obj:Destroy() end)
    end
    ESPObjects = {}
end

local function ToggleESP(state)
    State.ESP = state
    if state then
        ClearESP()
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") then
                if v.Name ~= Player.Name and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                    local color = Color3.new(1, 0, 0)
                    if string.find(string.lower(v.Name), "boss") then color = Color3.new(1, 0.5, 0) end
                    if string.find(string.lower(v.Name), "dragon") then color = Color3.new(1, 0.2, 0.8) end
                    if string.find(string.lower(v.Name), "levi") then color = Color3.new(0, 1, 1) end
                    if string.find(string.lower(v.Name), "elite") then color = Color3.new(0.5, 0, 1) end
                    CreateESP(v.HumanoidRootPart, color, v.Name .. " HP" .. math.floor(v.Humanoid.Health))
                end
                if string.find(string.lower(v.Name), "chest") then
                    CreateESP(v.HumanoidRootPart, Color3.new(1, 1, 0), "📦 CHEST")
                end
                if string.find(string.lower(v.Name), "fruit") then
                    CreateESP(v.HumanoidRootPart, Color3.new(0.5, 0, 1), "🍎 FRUIT")
                end
                if string.find(string.lower(v.Name), "factory") then
                    CreateESP(v.HumanoidRootPart, Color3.new(1, 0.5, 0.5), "🏭 FACTORY")
                end
            end
        end
    else
        ClearESP()
    end
end

-- ===== FLY =====
local function ToggleFly(state)
    State.Fly = state
    if state then
        if FlyBV then FlyBV:Destroy() end
        if FlyBG then FlyBG:Destroy() end
        
        FlyBV = Instance.new("BodyVelocity")
        FlyBV.MaxForce = Vector3.new(4000, 4000, 4000)
        FlyBV.Velocity = Vector3.new(0, Settings.FlySpeed, 0)
        FlyBV.Parent = Character.HumanoidRootPart
        
        FlyBG = Instance.new("BodyGyro")
        FlyBG.MaxTorque = Vector3.new(4000, 4000, 4000)
        FlyBG.P = 5000
        FlyBG.CFrame = Character.HumanoidRootPart.CFrame
        FlyBG.Parent = Character.HumanoidRootPart
    else
        if FlyBV then FlyBV:Destroy(); FlyBV = nil end
        if FlyBG then FlyBG:Destroy(); FlyBG = nil end
    end
end

-- ===== SPEED =====
local function ToggleSpeed(state)
    State.Speed = state
    if state then
        Humanoid.WalkSpeed = Settings.WalkSpeed
        Humanoid.JumpPower = Settings.JumpPower
    else
        Humanoid.WalkSpeed = 16
        Humanoid.JumpPower = 50
    end
end

-- ===== INFINITE JUMP =====
local function ToggleInfiniteJump(state)
    State.InfiniteJump = state
end

-- ===== NO CLIP =====
local function ToggleNoClip(state)
    State.NoClip = state
    for _, v in pairs(Character:GetDescendants()) do
        if v:IsA("BasePart") then
            v.CanCollide = not state
        end
    end
end

-- ===== WATER WALK =====
local function ToggleWaterWalk(state)
    State.WaterWalk = state
end

-- ===== NO FALL DAMAGE =====
local function ToggleNoFall(state)
    State.NoFall = state
end

-- ===== AUTO FUNCTIONS =====
local function StartAutoFarm()
    if State.AutoFarm then return end
    State.AutoFarm = true
    Notify("⚔️ Auto Farm Started")
    
    Threads.AutoFarm = task.spawn(function()
        while State.AutoFarm do
            local target = GetNearestNPC(Settings.FarmRadius, true)
            if target then
                AttackTarget(target)
            end
            wait(0.3)
        end
    end)
end

local function StartAutoV4()
    if State.AutoV4 then return end
    State.AutoV4 = true
    Notify("🌀 Auto V4 Started")
    
    Threads.AutoV4 = task.spawn(function()
        while State.AutoV4 do
            local npc = workspace:FindFirstChild("AwekeningNPC") or workspace:FindFirstChild("RaceNPC") or workspace:FindFirstChild("V4NPC")
            if npc and npc:FindFirstChild("HumanoidRootPart") then
                Teleport(npc.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                wait(0.3)
                FireClick(npc:FindFirstChildOfClass("ClickDetector"))
                for _, v in pairs(npc:GetDescendants()) do
                    if v:IsA("ClickDetector") then FireClick(v) end
                end
            end
            
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "v4") or string.find(string.lower(v.Name), "awaken") or string.find(string.lower(v.Name), "race")) then
                    if v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                        wait(0.2)
                        FireClick(v:FindFirstChildOfClass("ClickDetector"))
                        for _, child in pairs(v:GetDescendants()) do
                            if child:IsA("ClickDetector") then FireClick(child) end
                        end
                    end
                end
            end
            wait(Settings.V4Delay)
        end
    end)
end

local function StartFindLevi()
    if State.FindLevi then return end
    State.FindLevi = true
    Notify("🔍 Searching Leviathan")
    
    Threads.FindLevi = task.spawn(function()
        while State.FindLevi do
            local found = false
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "levi") or string.find(string.lower(v.Name), "leviathan")) then
                    if v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 10, 0))
                        Notify("🐉 Leviathan Found!", 5)
                        found = true
                        break
                    end
                end
            end
            
            if not found then
                local boat = workspace:FindFirstChild("Boat") or workspace:FindFirstChild("Ship") or workspace:FindFirstChild("SeaBoat")
                if boat and boat:FindFirstChild("HumanoidRootPart") then
                    Teleport(boat.HumanoidRootPart.Position + Vector3.new(0, 5, 0))
                end
                wait(Settings.LeviSearchInterval)
            else
                wait(30)
            end
        end
    end)
end

local function StartAutoSeaBeast()
    if State.AutoSeaBeast then return end
    State.AutoSeaBeast = true
    Notify("🌊 Auto Sea Beast Started")
    
    Threads.AutoSeaBeast = task.spawn(function()
        while State.AutoSeaBeast do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "seabeast") or string.find(string.lower(v.Name), "sea beast") or string.find(string.lower(v.Name), "kraken")) then
                    if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, Settings.SeaBeastHeight, 0))
                        local tool = EquipBestTool("fruit") or EquipBestTool("sword") or EquipBestTool("gun")
                        if tool then
                            for i = 1, 4 do
                                tool:Activate()
                                wait(0.25)
                            end
                        end
                    end
                end
            end
            wait(0.8)
        end
    end)
end

local function StartAutoRaid()
    if State.AutoRaid then return end
    State.AutoRaid = true
    Notify("⚔️ Auto Raid Started")
    
    Threads.AutoRaid = task.spawn(function()
        while State.AutoRaid do
            local npc = workspace:FindFirstChild("RaidNPC") or workspace:FindFirstChild("Raid")
            if npc and npc:FindFirstChild("HumanoidRootPart") then
                Teleport(npc.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                wait(0.3)
                FireClick(npc:FindFirstChildOfClass("ClickDetector"))
                for _, v in pairs(npc:GetDescendants()) do
                    if v:IsA("ClickDetector") then FireClick(v) end
                end
            end
            
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and string.find(string.lower(v.Name), "chest") and v:FindFirstChild("HumanoidRootPart") then
                    Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                    wait(0.2)
                    FireClick(v:FindFirstChildOfClass("ClickDetector"))
                end
            end
            wait(4)
        end
    end)
end

local function StartAutoChest()
    if State.AutoChest then return end
    State.AutoChest = true
    Notify("📦 Auto Chest Started")
    
    Threads.AutoChest = task.spawn(function()
        while State.AutoChest do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "chest") or string.find(string.lower(v.Name), "box") or string.find(string.lower(v.Name), "crate")) then
                    if v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                        wait(0.15)
                        FireClick(v:FindFirstChildOfClass("ClickDetector"))
                        for _, child in pairs(v:GetDescendants()) do
                            if child:IsA("ClickDetector") then FireClick(child) end
                        end
                    end
                end
            end
            wait(Settings.ChestDelay)
        end
    end)
end

local function StartAutoBounty()
    if State.AutoBounty then return end
    State.AutoBounty = true
    Notify("💰 Auto Bounty Started")
    
    Threads.AutoBounty = task.spawn(function()
        while State.AutoBounty do
            local target = GetNearestPlayer(500)
            if target then
                AttackTarget(target)
            else
                local npc = GetNearestNPC(300, true)
                if npc then AttackTarget(npc) end
            end
            wait(Settings.BountyDelay)
        end
    end)
end

local function StartAutoFruit()
    if State.AutoFruit then return end
    State.AutoFruit = true
    Notify("🍎 Auto Fruit Started")
    
    Threads.AutoFruit = task.spawn(function()
        while State.AutoFruit do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and string.find(string.lower(v.Name), "fruit") then
                    local root = v:FindFirstChild("HumanoidRootPart") or v:FindFirstChild("Handle") or v:FindFirstChildWhichIsA("BasePart")
                    if root then
                        Teleport(root.Position + Vector3.new(0, 2, 0))
                        wait(0.3)
                        local click = v:FindFirstChildOfClass("ClickDetector")
                        if click then FireClick(click) end
                    end
                end
            end
            wait(Settings.FruitDelay)
        end
    end)
end

local function StartAutoDungeon()
    if State.AutoDungeon then return end
    State.AutoDungeon = true
    Notify("🏰 Auto Dungeon Started")
    
    Threads.AutoDungeon = task.spawn(function()
        while State.AutoDungeon do
            local dungeonNPC = workspace:FindFirstChild("DungeonNPC") or workspace:FindFirstChild("Dungeon")
            if dungeonNPC and dungeonNPC:FindFirstChild("HumanoidRootPart") then
                Teleport(dungeonNPC.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                wait(0.3)
                FireClick(dungeonNPC:FindFirstChildOfClass("ClickDetector"))
            end
            
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "dungeon") or string.find(string.lower(v.Name), "portal")) then
                    if v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                        FireClick(v:FindFirstChildOfClass("ClickDetector"))
                    end
                end
            end
            wait(3)
        end
    end)
end

local function StartAutoAwaken()
    if State.AutoAwaken then return end
    State.AutoAwaken = true
    Notify("💫 Auto Awaken Started")
    
    Threads.AutoAwaken = task.spawn(function()
        while State.AutoAwaken do
            local awakenNPC = workspace:FindFirstChild("AwakeningNPC") or workspace:FindFirstChild("Awaken")
            if awakenNPC and awakenNPC:FindFirstChild("HumanoidRootPart") then
                Teleport(awakenNPC.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                wait(0.3)
                FireClick(awakenNPC:FindFirstChildOfClass("ClickDetector"))
            end
            wait(2)
        end
    end)
end

local function StartAutoQuest()
    if State.AutoQuest then return end
    State.AutoQuest = true
    Notify("📜 Auto Quest Started")
    
    Threads.AutoQuest = task.spawn(function()
        while State.AutoQuest do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "quest") or string.find(string.lower(v.Name), "npc")) then
                    if v:FindFirstChild("HumanoidRootPart") then
                        Teleport(v.HumanoidRootPart.Position + Vector3.new(0, 3, 0))
                        wait(0.2)
                        FireClick(v:FindFirstChildOfClass("ClickDetector"))
                    end
                end
            end
            wait(2)
        end
    end)
end

local function StartAutoCollect()
    if State.AutoCollect then return end
    State.AutoCollect = true
    Notify("🔄 Auto Collect Started")
    
    Threads.AutoCollect = task.spawn(function()
        while State.AutoCollect do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Model") and (string.find(string.lower(v.Name), "drop") or string.find(string.lower(v.Name), "loot") or string.find(string.lower(v.Name), "item")) then
                    local root = v:FindFirstChild("HumanoidRootPart") or v:FindFirstChildWhichIsA("BasePart")
                    if root then
                        Teleport(root.Position + Vector3.new(0, 2, 0))
                        wait(0.1)
                    end
                end
            end
            wait(0.5)
        end
    end)
end

local function StartAutoBoss()
    if State.AutoBoss then return end
    State.AutoBoss = true
    Notify("👑 Auto Boss Hunt Started")
    
    Threads.AutoBoss = task.spawn(function()
        while State.AutoBoss do
            local target = GetNearestNPC(500, true, true)
            if target then
                AttackTarget(target)
            end
            wait(1)
        end
    end)
end

local function StartAutoMastery()
    if State.AutoMastery then return end
    State.AutoMastery = true
    Notify("⚡ Auto Mastery Started")
    
    Threads.AutoMastery = task.spawn(function()
        while State.AutoMastery do
            local target = GetNearestNPC(Settings.FarmRadius, true)
            if target then
                AttackTarget(target)
                wait(0.1)
            end
            wait(0.5)
        end
    end)
end

-- ===== LOCATIONS =====
local Locations = {
    {"Marine", Vector3.new(-1200, 15, 2400)},
    {"Pirate", Vector3.new(1200, 15, -2400)},
    {"Jungle", Vector3.new(-500, 20, -500)},
    {"Desert", Vector3.new(1500, 20, 1500)},
    {"Snow", Vector3.new(-2000, 20, -2000)},
    {"Kingdom S2", Vector3.new(0, 100, 0)},
    {"Castle S2", Vector3.new(500, 50, 500)},
    {"Elite S3", Vector3.new(-500, 30, -1500)},
    {"Floating S3", Vector3.new(1500, 300, 0)},
    {"Dragon", Vector3.new(-2000, 50, 2000)},
    {"Ice", Vector3.new(2500, 20, -2500)},
    {"Dark Arena", Vector3.new(0, 0, 3000)},
    {"Volcano", Vector3.new(-1500, 50, -1500)},
    {"Graveyard", Vector3.new(800, 20, -800)},
    {"Factory", Vector3.new(0, 20, 2000)},
    {"Cafe", Vector3.new(-100, 10, 100)}
}

-- ===== MENU UI =====
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = Player:WaitForChild("PlayerGui")
ScreenGui.Name = "BananaMenuV2"

-- ===== FLOATING BUTTON =====
local FloatingButton = Instance.new("ImageButton")
FloatingButton.Size = UDim2.new(0, 55, 0, 55)
FloatingButton.Position = UDim2.new(0.02, 0, 0.85, 0)
FloatingButton.BackgroundColor3 = Color3.new(0.2, 0.1, 0.05)
FloatingButton.BackgroundTransparency = 0.15
FloatingButton.Image = "rbxassetid://7733992528"
FloatingButton.ImageColor3 = Color3.new(1, 0.8, 0.1)
FloatingButton.ZIndex = 999
FloatingButton.Parent = ScreenGui

local FBcorner = Instance.new("UICorner")
FBcorner.CornerRadius = UDim.new(1, 0)
FBcorner.Parent = FloatingButton

local FBstroke = Instance.new("UIStroke")
FBstroke.Color = Color3.new(1, 0.8, 0.1)
FBstroke.Transparency = 0.5
FBstroke.Thickness = 2
FBstroke.Parent = FloatingButton

-- ===== MAIN MENU =====
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 400, 0, 580)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -290)
MainFrame.BackgroundColor3 = Color3.new(0.05, 0.03, 0.08)
MainFrame.BackgroundTransparency = 0.08
MainFrame.BorderSizePixel = 0
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 15)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.new(1, 0.8, 0.1)
MainStroke.Transparency = 0.3
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- ===== TITLE BAR =====
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 50)
TitleBar.BackgroundColor3 = Color3.new(0.1, 0.06, 0.02)
TitleBar.BackgroundTransparency = 0.3
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 15)
TitleCorner.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(0.7, 0, 1, 0)
TitleLabel.Position = UDim2.new(0.02, 0, 0, 0)
TitleLabel.Text = "🍌 BANANA MENU V2"
TitleLabel.TextColor3 = Color3.new(1, 0.8, 0.1)
TitleLabel.BackgroundTransparency = 1
TitleLabel.TextScaled = true
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

local SubLabel = Instance.new("TextLabel")
SubLabel.Size = UDim2.new(0.7, 0, 0.5, 0)
SubLabel.Position = UDim2.new(0.02, 0, 0.5, 0)
SubLabel.Text = "Ultimate Mobile | Delta/VNG/Arceus"
SubLabel.TextColor3 = Color3.new(0.5, 0.5, 0.5)
SubLabel.BackgroundTransparency = 1
SubLabel.TextScaled = true
SubLabel.Font = Enum.Font.Gotham
SubLabel.TextXAlignment = Enum.TextXAlignment.Left
SubLabel.Parent = TitleBar

local CloseBtn = Instance.new("ImageButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(0.94, 0, 0.1, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Image = "rbxassetid://79324227570635"
CloseBtn.ImageColor3 = Color3.new(1, 0.3, 0.3)
CloseBtn.Parent = TitleBar

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    MenuVisible = false
end)

-- ===== TAB SYSTEM =====
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1, -10, 0, 35)
TabFrame.Position = UDim2.new(0, 5, 0, 52)
TabFrame.BackgroundTransparency = 1
TabFrame.Parent = MainFrame

local Tabs = {"Combat", "Progress", "Special", "Movement", "Utility"}
local CurrentTab = "Combat"

local function CreateTabButton(text, pos)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.18, 0, 1, 0)
    btn.Position = UDim2.new(pos, 0, 0, 0)
    btn.Text = text
    btn.TextColor3 = Color3.new(0.7, 0.7, 0.7)
    btn.BackgroundColor3 = Color3.new(0.1, 0.06, 0.02)
    btn.BackgroundTransparency = 0.5
    btn.BorderSizePixel = 0
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamBold
    btn.Parent = TabFrame
    
    local cr = Instance.new("UICorner")
    cr.CornerRadius = UDim.new(0, 8)
    cr.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        CurrentTab = text
        for _, child in pairs(TabFrame:GetChildren()) do
            if child:IsA("TextButton") then
                child.TextColor3 = Color3.new(0.7, 0.7, 0.7)
                child.BackgroundColor3 = Color3.new(0.1, 0.06, 0.02)
            end
        end
        btn.TextColor3 = Color3.new(1, 0.8, 0.1)
        btn.BackgroundColor3 = Color3.new(0.2, 0.1, 0.05)
        UpdateContent()
    end)
    
    if text == "Combat" then
        btn.TextColor3 = Color3.new(1, 0.8, 0.1)
        btn.BackgroundColor3 = Color3.new(0.2, 0.1, 0.05)
    end
    
    return btn
end

for i, tab in pairs(Tabs) do
    CreateTabButton(tab, 0.02 + (i - 1) * 0.2)
end

-- ===== CONTENT FRAME =====
local ContentFrame = Instance.new("ScrollingFrame")
ContentFrame.Size = UDim2.new(1, -10, 1, -100)
ContentFrame.Position = UDim2.new(0, 5, 0, 92)
ContentFrame.BackgroundTransparency = 1
ContentFrame.CanvasSize = UDim2.new(0, 0, 0, 1000)
ContentFrame.ScrollBarThickness = 3
ContentFrame.Parent = MainFrame

local ContentList = Instance.new("UIListLayout")
ContentList.Padding = UDim.new(0, 4)
ContentList.SortOrder = Enum.SortOrder.LayoutOrder
ContentList.Parent = ContentFrame

-- ===== UI FUNCTIONS =====
local function ClearContent()
    for _, child in pairs(ContentFrame:GetChildren()) do
        if child:IsA("TextLabel") or child:IsA("TextButton") or child:IsA("Frame") then
            child:Destroy()
        end
    end
end

local function CreateSection(text)
    local section = Instance.new("TextLabel")
    section.Size = UDim2.new(1, -10, 0, 28)
    section.Text = "─── " .. text .. " ───"
    section.TextColor3 = Color3.new(1, 0.8, 0.1)
    section.BackgroundTransparency = 1
    section.TextScaled = true
    section.Font = Enum.Font.GothamBold
    section.Parent = ContentFrame
    return section
end

local function CreateButton(text, cb)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 36)
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.BackgroundColor3 = Color3.new(0.12, 0.08, 0.15)
    btn.BorderSizePixel = 0
    btn.TextScaled = true
    btn.Font = Enum.Font.Gotham
    btn.Parent = ContentFrame
    
    local cr = Instance.new("UICorner")
    cr.CornerRadius = UDim.new(0, 6)
    cr.Parent = btn
    
    btn.MouseButton1Click:Connect(cb)
    return btn
end

local function CreateToggle(text, stateVar, cb)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 36)
    frame.BackgroundColor3 = Color3.new(0.08, 0.05, 0.12)
    frame.BorderSizePixel = 0
    frame.Parent = ContentFrame
    
    local cr = Instance.new("UICorner")
    cr.CornerRadius = UDim.new(0, 6)
    cr.Parent = frame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Text = text
    label.TextColor3 = Color3.new(1, 1, 1)
    label.BackgroundTransparency = 1
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextScaled = true
    label.Font = Enum.Font.Gotham
    label.Parent = frame
    
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0.3, 0, 0.8, 0)
    toggle.Position = UDim2.new(0.7, 0, 0.1, 0)
    toggle.Text = "OFF"
    toggle.TextColor3 = Color3.new(1, 0.3, 0.3)
    toggle.BackgroundColor3 = Color3.new(0.2, 0.1, 0.15)
    toggle.BorderSizePixel = 0
    toggle.TextScaled = true
    toggle.Font = Enum.Font.GothamBold
    toggle.Parent = frame
    
    local cr2 = Instance.new("UICorner")
    cr2.CornerRadius = UDim.new(0, 5)
    cr2.Parent = toggle
    
    toggle.MouseButton1Click:Connect(function()
        stateVar = not stateVar
        toggle.Text = stateVar and "ON" or "OFF"
        toggle.TextColor3 = stateVar and Color3.new(0.2, 0.9, 0.2) or Color3.new(1, 0.3, 0.3)
        toggle.BackgroundColor3 = stateVar and Color3.new(0.1, 0.3, 0.1) or Color3.new(0.2, 0.1, 0.15)
        cb(stateVar)
    end)
    return toggle
end

-- ===== UPDATE CONTENT =====
local function UpdateContent()
    ClearContent()
    
    if CurrentTab == "Combat" then
        CreateSection("⚔️ COMBAT")
        CreateToggle("ESP", State.ESP, function(v) ToggleESP(v) end)
        CreateToggle("Kill Aura", State.KillAura, function(v) State.KillAura = v; Notify(v and "Kill Aura ON" or "Kill Aura OFF") end)
        CreateToggle("Aimbot", State.Aimbot, function(v) State.Aimbot = v; Notify(v and "Aimbot ON" or "Aimbot OFF") end)
        CreateToggle("Auto Farm", State.AutoFarm, function(v) if v then StartAutoFarm() else State.AutoFarm = false; Notify("Farm Stopped") end end)
        CreateToggle("Auto Bounty", State.AutoBounty, function(v) if v then StartAutoBounty() else State.AutoBounty = false; Notify("Bounty Stopped") end end)
        CreateToggle("Auto Raid", State.AutoRaid, function(v) if v then StartAutoRaid() else State.AutoRaid = false; Notify("Raid Stopped") end end)
        CreateToggle("Auto Sea Beast", State.AutoSeaBeast, function(v) if v then StartAutoSeaBeast() else State.AutoSeaBeast = false; Notify("Sea Beast Stopped") end end)
        CreateToggle("Auto Boss Hunt", State.AutoBoss, function(v) if v then StartAutoBoss() else State.AutoBoss = false; Notify("Boss Hunt Stopped") end end)
        
    elseif CurrentTab == "Progress" then
        CreateSection("📈 PROGRESS")
        CreateToggle("Auto V4", State.AutoV4, function(v) if v then StartAutoV4() else State.AutoV4 = false; Notify("V4 Stopped") end end)
        CreateToggle("Auto Awaken", State.AutoAwaken, function(v) if v then StartAutoAwaken() else State.AutoAwaken = false; Notify("Awaken Stopped") end end)
        CreateToggle("Auto Mastery", State.AutoMastery, function(v) if v then StartAutoMastery() else State.AutoMastery = false; Notify("Mastery Stopped") end end)
        CreateToggle("Auto Level", State.AutoLevel, function(v) State.AutoLevel = v; Notify(v and "Auto Level ON" or "Auto Level OFF") end)
        CreateToggle("Auto Stats", State.AutoStats, function(v) State.AutoStats = v; Notify(v and "Auto Stats ON" or "Auto Stats OFF") end)
        CreateToggle("Auto Quest", State.AutoQuest, function(v) if v then StartAutoQuest() else State.AutoQuest = false; Notify("Quest Stopped") end end)
        CreateToggle("Auto Dungeon", State.AutoDungeon, function(v) if v then StartAutoDungeon() else State.AutoDungeon = false; Notify("Dungeon Stopped") end end)
        
    elseif CurrentTab == "Special" then
        CreateSection("🔍 SPECIAL")
        CreateToggle("Find Leviathan", State.FindLevi, function(v) if v then StartFindLevi() else State.FindLevi = false; Notify("Levi Search Stopped") end end)
        CreateToggle("Auto Fruit", State.AutoFruit, function(v) if v then StartAutoFruit() else State.AutoFruit = false; Notify("Fruit Stopped") end end)
        CreateToggle("Auto Chest", State.AutoChest, function(v) if v then StartAutoChest() else State.AutoChest = false; Notify("Chest Stopped") end end)
        CreateToggle("Auto Collect", State.AutoCollect, function(v) if v then StartAutoCollect() else State.AutoCollect = false; Notify("Collect Stopped") end end)
        CreateToggle("Auto Stock", State.AutoStock, function(v) State.AutoStock = v; Notify(v and "Auto Stock ON" or "Auto Stock OFF") end)
        CreateToggle("Auto Hatch", State.AutoHatch, function(v) State.AutoHatch = v; Notify(v and "Auto Hatch ON" or "Auto Hatch OFF") end)
        CreateToggle("Auto Fishing", State.AutoFishing, function(v) State.AutoFishing = v; Notify(v and "Auto Fishing ON" or "Auto Fishing OFF") end)
        
    elseif CurrentTab == "Movement" then
        CreateSection("🕹️ MOVEMENT")
        CreateToggle("Fly", State.Fly, function(v) ToggleFly(v) end)
        CreateToggle("Speed", State.Speed, function(v) ToggleSpeed(v) end)
        CreateToggle("Infinite Jump", State.InfiniteJump, function(v) ToggleInfiniteJump(v) end)
        CreateToggle("No Clip", State.NoClip, function(v) ToggleNoClip(v) end)
        CreateToggle("Water Walk", State.WaterWalk, function(v) ToggleWaterWalk(v) end)
        CreateToggle("No Fall", State.NoFall, function(v) ToggleNoFall(v) end)
        
        CreateSection("📍 TELEPORT")
        for _, loc in pairs(Locations) do
            CreateButton("TP " .. loc[1], function()
                Teleport(loc[2])
                Notify("TP to " .. loc[1])
            end)
        end
        CreateButton("Custom TP", function()
            local x = tonumber(game:GetService("StarterGui"):SetCore("Prompt", {Title = "X", Text = "Enter X"})) or 0
            local y = tonumber(game:GetService("StarterGui"):SetCore("Prompt", {Title = "Y", Text = "Enter Y"})) or 0
            local z = tonumber(game:GetService("StarterGui"):SetCore("Prompt", {Title = "Z", Text = "Enter Z"})) or 0
            Teleport(Vector3.new(x, y, z))
            Notify("TP to custom position")
        end)
        
    elseif CurrentTab == "Utility" then
        CreateSection("🛠️ UTILITY")
        CreateToggle("Auto Heal", State.AutoHeal, function(v) State.AutoHeal = v; Notify(v and "Auto Heal ON" or "Auto Heal OFF") end)
        CreateToggle("Auto Buff", State.AutoBuff, function(v) State.AutoBuff = v; Notify(v and "Auto Buff ON" or "Auto Buff OFF") end)
        CreateToggle("Anti Stun", State.AntiStun, function(v) State.AntiStun = v; Notify(v and "Anti Stun ON" or "Anti Stun OFF") end)
        CreateToggle("Anti AFK", State.AntiAFK, function(v) State.AntiAFK = v; Notify(v and "Anti AFK ON" or "Anti AFK OFF") end)
        CreateToggle("Anti Ban", State.AntiBan, function(v) State.AntiBan = v; Notify(v and "Anti Ban ON" or "Anti Ban OFF") end)
        CreateToggle("Anti Lag", State.AntiLag, function(v) State.AntiLag = v; Notify(v and "Anti Lag ON" or "Anti Lag OFF") end)
        CreateToggle("Auto Rejoin", State.AutoRejoin, function(v) State.AutoRejoin = v; Notify(v and "Auto Rejoin ON" or "Auto Rejoin OFF") end)
        
        CreateSection("⚙️ CONTROLS")
        CreateButton("Refresh Character", function() Player:LoadCharacter(); Notify("Refreshed") end)
        CreateButton("Rejoin Server", function() game:GetService("TeleportService"):Teleport(game.PlaceId, Player) end)
        CreateButton("Set GUI Keybind", function()
            local key = UserInputService:GetLastInput().KeyCode.Name
            Keybinds[key] = function()
                MainFrame.Visible = not MainFrame.Visible
                MenuVisible = MainFrame.Visible
            end
            Notify("Keybind set to " .. key)
        end)
        CreateButton("Hide GUI", function()
            MainFrame.Visible = false
            MenuVisible = false
        end)
    end
end

-- ===== FLOATING BUTTON DRAG =====
FloatingButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    MenuVisible = MainFrame.Visible
end)

FloatingButton.MouseButton1Down:Connect(function()
    Dragging = true
    DragStart = Vector2.new(UserInputService:GetMouseLocation().X, UserInputService:GetMouseLocation().Y)
    DragOffset = Vector2.new(FloatingButton.Position.X.Offset, FloatingButton.Position.Y.Offset)
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        Dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if not Dragging or input.UserInputType ~= Enum.UserInputType.MouseMovement and input.UserInputType ~= Enum.UserInputType.Touch then
        return
    end
    local currentPos = UserInputService:GetMouseLocation()
    local delta = currentPos - DragStart
    local newX = math.clamp(DragOffset.X + delta.X, 0, 1 - FloatingButton.Size.X.Scale)
    local newY = math.clamp(DragOffset.Y + delta.Y, 0, 1 - FloatingButton.Size.Y.Scale)
    FloatingButton.Position = UDim2.new(0, newX, 0, newY)
end)

-- ===== KEYBIND LISTENER =====
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    local key = input.KeyCode.Name
    for k, fn in pairs(Keybinds) do
        if k == key then fn() end
    end
end)

-- ===== INFINITE JUMP LOOP =====
RunService.Heartbeat:Connect(function()
    if State.InfiniteJump and Humanoid then
        Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- ===== ESP REFRESH =====
RunService.RenderStepped:Connect(function()
    if State.ESP and #ESPObjects < 5 then
        ToggleESP(true)
    end
end)

-- ===== ANTI AFK =====
RunService.Heartbeat:Connect(function()
    if State.AntiAFK and Character and Character:FindFirstChild("HumanoidRootPart") then
        Character.HumanoidRootPart.Velocity = Vector3.new(0, 0.01, 0)
    end
end)

-- ===== NO FALL =====
RunService.Heartbeat:Connect(function()
    if State.NoFall and Character and Character:FindFirstChild("HumanoidRootPart") then
        if Humanoid and Humanoid:GetState() == Enum.HumanoidStateType.Falling then
            Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

-- ===== WATER WALK =====
RunService.Heartbeat:Connect(function()
    if State.WaterWalk and Character and Character:FindFirstChild("HumanoidRootPart") then
        local hrp = Character.HumanoidRootPart
        if hrp.Position.Y < 0 and hrp.Velocity.Y < 0 then
            hrp.Velocity = Vector3.new(hrp.Velocity.X, 0, hrp.Velocity.Z)
        end
    end
end)

-- ===== KILL AURA =====
RunService.Heartbeat:Connect(function()
    if State.KillAura then
        local targets = {}
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Model") and v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") then
                if v.Name ~= Player.Name and v.Humanoid.Health > 0 then
                    local mag = (v.HumanoidRootPart.Position - Character.HumanoidRootPart.Position).Magnitude
                    if mag < 50 then
                        table.insert(targets, v)
                    end
                end
            end
        end
        for _, target in pairs(targets) do
            AttackTarget(target)
        end
    end
end)

-- ===== INIT =====
UpdateContent()
Notify("🍌 Banana Menu V2 Loaded!", 4)
print("[BANANA MENU V2] Loaded successfully!")
